# 工程总览 · stock-predictor（快速了解）

> ⚠️ **红线**：仅供交流与学习，**不构成投资建议、绝不荐股**；作者与本产品**不承担任何法律责任与风险**，盈亏自负。

一个**单文件**（`stock_predictor.py`，约 1.18 万行）的 A 股预测研究平台。核心信条：**真实数据、诚实评估、绝不造假**。
本文件给你 5 分钟看懂整个工程；详细规范见 [README.md](README.md)、给 AI 协作者见 [AGENTS.md](AGENTS.md)。

---

## 一句话
拉真实行情+外部数据 → 造特征 → 35 种算法训练（训练/验证/测试三分不泄露）→ 多周期涨跌预测 →
方向准确率/收益回测诚实评估 → 风险提示 → 综合报告 → 实时监控 → **把预测存下来等日期到了对比真实股价算真实准确率**。

## 三条铁律（贯穿全程）
1. **不造假**：任何数据缺失就如实"未接入"，绝不填假值/编情绪分/编因果。
2. **不泄露**：时间序列严格按时间三分；标准化只在训练集 fit；外部数据按日期向后对齐；**交叉验证用前扩窗口 walk-forward**（不拿未来折训练）。
3. **不吹准**：默认预测涨跌幅（治 R² 虚高）；强制放 Naive/总是涨双基准；DA 做**二项显著性检验**、比多个模型再挑最好的会给**多重比较偏差**警示；真实股票上 DA≈50% 就显示 50%。

---

## 数据来源（全部公开、可溯源、界面可点链接核对）
| 数据 | 来源 | 接口 |
|---|---|---|
| 日线行情 | 东方财富 / baostock（多源自动容错） | `stock_zh_a_hist` / baostock |
| 财报估值(PE/PB/市值) | 乐咕乐股 / 百度（自动适配） | `stock_a_indicator_lg` / `stock_zh_valuation_baidu` |
| 大盘环境 | 东方财富(沪深300) | `stock_zh_index_daily` |
| 主力资金流 | 东方财富(带浏览器头直连) | 东财资金流 API |
| 隔夜美股/北向资金 | 新浪/东方财富 | `index_us_stock_sina` / `stock_hsgt_north_net_flow_in_em` |
| 个股新闻 | 东方财富 | `stock_news_em`（仅展示+风险关键词筛查，不作训练特征） |
| 实时盘口 | 东方财富 | 盘口五档（监控用，自采集记录到 monitor_log/） |
| 交易日历 | 东方财富(新浪) | `tool_trade_date_hist_sina`（真实节假日，定未来日期/到期日） |

周/月/季多周期趋势、量价情绪、主力意图代理均由日线滚动计算（无泄露）。

## 界面（页签）
页签按**板块二级导航**分组（先选板块再选页面，见 `_build_tab_nav`）：
- 行情板块：K线图(日3月/周1.5年/月6年·可缩放·区间平均线，工具条可折叠) ｜ 板块行情
- 预测板块：预测对比 ｜ **未来预测(默认未来一周·逐日+置信区间+策略信号快照+自动同步进预测跟踪)** ｜ 策略回测(带手续费+A股涨跌停/停牌/T+1+波动率目标仓位/止损+对比大盘)
- 机器学习板块：机器学习内部(数据透明+溯源+风险提示)
- 精度评估板块：指标结果(训/验/测 + **模型推荐与预测依据+多重比较警示**) ｜ **预测跟踪(存预测→到期对比真实→区分方向命中/股价命中，仅留最近15日已验证记录)** ｜ 综合报告(汇总+导出HTML+模型机械倾向)
- 实盘操作板块：实时监控(盘口+记录) ｜ **尾盘选股(五维筛选+历史有效性验证)** ｜ 批量扫描 ｜ 组合与仓位(1%风险计算器+金字塔建仓)
  ｜ **监管披露观察(大宗交易折溢率+大股东增减持+龙虎榜三项强制披露信号交叉验证)** ｜ **模拟交易(纸面模拟盘，按A股真实规则逐周期自动买卖)**
- 日志板块：运行日志 ｜ 操作日志

模型下拉（策略回测/预测跟踪/未来预测）只列**本次训练成功**的模型，避免选到未训练模型。每个重操作都有醒目进程弹窗。

## 代码结构（stock_predictor.py 分节）
- 一~二 依赖/配置（`TrainConfig`）
- 三 数据获取 `StockDataFetcher`（多源/外部/实时/新闻/估值）
- 四 特征工程 `FeatureEngineer`（技术指标+多周期+情绪+主力+三分）
- 五 评价指标 `Metrics`（含 DA 方向准确率 / UP_P 上涨精确率）
- 六 35 算法 + 注册表 `ALGO_REGISTRY`
- 七 超参优化（PSO/GA/FA/SOA/BO）
- 八 训练调度 `TrainingPipeline`（CV=前扩窗口）+ 回测 `backtest_directional`（A股制度+风控+对比大盘+Beta/Jensen's Alpha）
     + `board_limit_pct` + 未来预测 `forecast_curve`（含置信区间）+ 交易日历 `next_trading_days`
     + `da_significance`（二项检验）+ 风险 `assess_risks` + 批量 `batch_scan` + 因子 `batch_factor_scan`（6因子，含盈余质量/税率异常预警）
     + `optimize_factor_weights`（岭回归学因子权重）+ 预测跟踪 `save/verify_predictions`
     + `calibration_check`（概率校准+显著性+多重比较）+ 尾盘选股 `tail_market_scan` + 监管观察名单 `regulatory_watchlist`
     + 模拟交易账户 `create/run_paper_account`（纸面模拟盘，A股真实规则逐周期自动买卖）
- 九 GUI（`MainWindow`，13 页签）
- 十~十一 可编程 API `run_experiment` + CLI/GUI 入口

## 快速开始
```bash
pip install -r requirements.txt          # 或用清华镜像：加 -i https://pypi.tuna.tsinghua.edu.cn/simple
python stock_predictor.py                # 图形界面
python stock_predictor.py --cli --code 600519 --week --backtest   # 命令行(未来一周逐日+回测)
```

## 预测到底准不准？（诚实回答）
真实 A 股上，多数模型方向准确率 **48%~54%（≈抛硬币）**，扣手续费回测多**跑不赢买入持有**。
这不是软件差，是**市场接近有效**的真相。本软件价值不在"准"，在于：①诚实告诉你准不准（不自欺）
②风险提示帮你避雷（退市/亏损/暴跌/负面新闻）③学量化研究的正规方法。**任何保证高准确率的都是骗子。**

## 路线图
- [x] 隔夜美股 / 北向资金 作为特征
- [x] 因子打分选股（5 因子：价值/动量/资金/质量/低波动）
- [x] 未来一周逐日预测 + 真实交易日历 + 置信区间
- [x] 回测贴合 A 股制度（涨跌停/停牌/T+1）+ 风控（波动率目标仓位/止损）+ 对比大盘
- [x] DA 显著性检验 + 多重比较偏差警示 + CV 前扩窗口
- [x] 尾盘五维选股历史有效性验证（回放五维条件配对未来收益 + DA 二项检验）
- [x] 大宗交易 / 大股东增减持 / 龙虎榜接入 + 监管披露观察名单策略
- [x] 方向概率输出原型 + 校准检验（分桶显著性 + 多重比较校验）
- [x] 会计学盈余质量三因子 + 有效税率异常筛查（经营现金流/净利润、应收账款与存货周转天数同比）
- [x] 因子权重优化（岭回归，价值/动量/低波动三因子）+ Beta/Jensen's Alpha 业绩归因
- [x] 模拟交易账户（纸面模拟盘，A股真实规则逐周期自动买卖，对比大盘）
- [ ] ARIMA 改 walk-forward 一步预测
- [ ] 真·舆情情绪（新闻 NLP，须真抓不臆造）
- [ ] 组合层面回测 / 行业中性；融资融券 / 解禁日历因子

## 对齐量化行业技术标准（参考量化招聘 JD 技术要求）
数据处理/可视化(热力图/IC/消融/宏观图)/**回测框架(自研+独立复算+Backtrader三方交叉验证)**/算法统计/
**行业划分(行业内估值分位)**/交易规则(涨跌停·T+1·停牌·真实交易日历)/**北交所代码识别**/宏观分析(仅研究不入训练)/
**Linux·Docker·Bash自动化**/**反数据泄漏Checklist**——逐条工程化落地，均可运行可复现，详见 README「对齐量化行业技术标准」节。

## 借鉴的开源 Skills（致谢，仅参考公开方法学、未复制代码）
- **staskh/trading_skills**：Piotroski F-Score、财报披露日历
- **yennanliu/InvestSkill**：基本面结构化分析(Piotroski/质量)
- **agiprolabs/claude-trading-skills**：凯利仓位、相关性/组合分散化、因子 IC/ICIR、Walk-Forward、EWMA/GARCH 波动率、均值回归股性(Hurst)
- **tradermonty/claude-trading-skills**：相对强弱 RS、大盘状态 regime
> 均为标准公开公式，按 A 股 + 诚实优先重写；只用于陈列事实/验证真伪，不产出买卖信号。

## 合规（重要）
**仅供交流与学习，不构成投资建议、绝不荐股；作者与本产品不承担任何法律责任与风险，盈亏自负。**
请自用、勿高频抓取、勿无牌荐股/售卖数据。做成对外产品前请咨询证券合规律师。
