# 使用教程（简易版）

> 面向不太懂编程/金融的用户。跟着做即可跑起来。
> **⚠ 本软件仅供学习研究，不构成投资建议、不荐股；版权归作者所有，未经书面许可禁止售卖/商用（见 [LICENSE](LICENSE)）。**
>
> 想配图：把你的界面截图放进 `docs/images/` 文件夹，在下面对应步骤按 `![说明](docs/images/文件名.png)` 引用即可。

---

## 0. 一句话认识它
填一个股票代码 → 勾几个模型 → 点"运行" → 看它对**明天涨还是跌**的判断、能不能**扣手续费还赚钱**。
它最大的特点是**诚实**：会明确告诉你"预测其实≈抛硬币"，绝不吹"稳赚"。

---

## 1. 安装
1. 装好 Python 3.9+。
2. 在软件所在文件夹打开终端，运行：
   ```bash
   pip install -r requirements.txt
   ```
   国内慢可加清华镜像：`-i https://pypi.tuna.tsinghua.edu.cn/simple`

> **Windows 用户名含 `&` 的注意**（如 `李鼎&office`）：别用 VS Code 的 ▶ 按钮，改在终端里
> `cd "带引号的路径"; python stock_predictor.py`，否则会报 `AmpersandNotAllowed`。

## 2. 启动
```bash
python stock_predictor.py
```
启动会弹一个"使用须知"窗口（合规红线 + 版权声明），看完点确定。

---

## 3. 第一次预测（3 步）
界面**左侧是配置区**，**右侧是结果标签页**。

1. **填股票代码**：左上"股票代码"框，填 6 位数字，如 `600519`（贵州茅台）。
2. **确认已勾模型**：左侧"多模型算法选择"里，默认已勾好 **SVR / GPR / Lasso / PLSR / ELM / ARIMA**（又快又相对准的 6 个，不用改）。
3. **点最下方蓝色大按钮**「运行选中配置模型」。等进度弹窗跑完即可。

> 训练完成后，几处"模型"下拉会**自动跳到方向准确率(DA)最高的那个模型**。

---

## 4. 看结果

### 4.1 指标结果表格
右侧「指标结果表格」页：每个模型一行，重点看 **DA（方向准确率）** 这列。
- **50% = 抛硬币**；只有明显 **> "总是涨"基准**、且统计显著，才算有点判别力。
- 每格是"训练 / 验证 / 测试"三个数——**只信测试那个**。

### 4.2 预测结果对比图（重要认知）
「预测结果对比图」页会看到两条线几乎重合——**这不是神准！**
> **『精度』≠『准确率』**：价格拟合精度(R²)能到 99%，但涨跌方向准确率(DA)只有 ~50%。
> 因为股票"明天价≈今天价"，照抄今天就有 99% 精度，但对方向毫无帮助。**别看那条贴合的线，只看 DA 和回测。**

---

## 5. 未来一周逐日预测
「未来预测图」页 → 默认"未来一周·逐日" → 点「🔮 预测未来走势」。
- 得到**下周一~周五每天**的预测价 + 涨跌% + **约80%置信区间**（阴影带）。
- 日期用**真实交易日历**（自动跳过节假日）。
- 越往后越不准，图上会标注。

---

## 6. 策略回测（照妖镜：能不能真赚钱）
「策略回测」页 → 选模型 → 点「▶ 回测」。
- 假设"模型说涨就买、说跌就空仓"走一遍历史（扣手续费），和"一直持有""大盘"比。
- **红线明显在蓝线之上、超额为正，才说明预测能赚钱**；跑不赢就说明没用。
- 已含 A 股制度：涨停买不进 / 跌停卖不出 / 停牌 / T+1。
- 点「🔬 交叉验证」可用**独立引擎 + Backtrader** 复核，证明回测数字是真的。

---

## 7. 综合研判卡（给普通人：依据 → 方向 → 目标价）
「综合报告」页 → 点「🎯 综合研判卡」。会按顺序给你：
1. **① 支持依据**（客观事实）：公司盈利/质量、主力资金、大盘环境、风险、近期新闻（可点链接）。
2. **② 模型方向判断**：📈偏涨 / 📉偏跌（并列它的可信度 DA）。
3. **③ 预测目标价** + 涨跌% + 约80%置信区间。

> ⚠ 这是"模型的机械预测"，**不是投资建议**；DA 不显著就当没有。

---

## 8. 预测跟踪（时间到了自动核对真实准确率）
「预测跟踪」页：
1. 点「📌 记录当前预测」→ 存下 1/3/5/10/20/30/40/60 日 8 个周期的预测。
2. **过些天再打开软件，会自动核对**已到期的预测（拉真实股价对比），更新真实准确率表——**无需手动操作**。
3. 表格给每个周期的**方向准确率 + 最大/平均/中位数/最小误差 + 区间覆盖率**，全是真实到期数据。

> 这是最诚实的一环：真实准确率靠时间自然长出来，不是回测瞬间造的漂亮数字。

---

## 9. 选股与组合（进阶）
- **「批量扫描」页**：填多只代码 → 批量预测/风险；或「🏆 因子打分选股」按价值/动量/资金/质量/低波动 + **行业内估值分位** 排名。
- **「组合与仓位」页**：
  - 相关性 + 分散化（**热力图**）；
  - **凯利公式**算仓位；
  - **因子有效性 IC**（柱状图，验证因子有没有用）；
  - **🌐 宏观快照**（GDP/CPI/PMI/M2 趋势，仅研究背景、不入训练）。
- **「机器学习内部」页**：最上面有**「量化输入→量化输出」总表**（哪些量化了、输出是什么）；还能做**因子消融实验**（看每类因子是否真带来提升）和 **Walk-Forward** 验证。

---

## 10. 命令行 / 定时任务（可选）
```bash
# 合成数据秒级自测
python stock_predictor.py --cli --synthetic --algos "RF,SVR,GBRT"
# 真实股票 + 未来一周逐日 + 回测
python stock_predictor.py --cli --code 600519 --week --backtest
```
Docker、每日自动核对预测的 Bash 脚本见 [README](README.md) 的"工程化"节。

---

## ⚠ 最重要的三句话
1. **市场接近有效，单模型方向准确率多数 ≈ 50%**——软件会如实告诉你，绝不吹"稳赚"。
2. **别看"精度99%"，只看 DA 和扣费回测**；能诚实报出 50% 才是真话。
3. **本软件不荐股、不构成投资建议，买不买你自己决定，盈亏自负。**
