# ============================================================================
# 核心依赖（必须安装，否则程序无法运行）
# ============================================================================
numpy>=1.23
pandas>=1.5
scikit-learn>=1.2
matplotlib>=3.6
pyarrow>=10.0            # 行情数据的本地 parquet 缓存需要

# ============================================================================
# 图形界面（想用 GUI 就装；只做命令行/脚本调用可不装）
# ============================================================================
PySide6>=6.5

# ============================================================================
# 真实 A 股数据源（想用真实数据至少装一个；两个都装可自动容错、更稳）
# ============================================================================
akshare>=1.12            # 东方财富源（主用）
baostock>=0.8.9          # 国内稳定备用源（免注册）；东方财富拉不到时自动切换到它

# ============================================================================
# 可选：更多算法（缺失会自动降级，不影响整体运行）
# ============================================================================
# xgboost>=1.7           # XGBoost 算法
# lightgbm>=3.3          # LightGBM 算法
# catboost>=1.2          # CatBoost 算法
# torch>=2.0             # LSTM/GRU/Transformer/CNN/DNN/ResNet/BPNet 等深度学习算法
# pytorch-tabnet>=4.0    # TabNet（缺失则用 DNN 近似替代）
# gplearn>=0.4           # 符号回归 SR / GEP / MEP 近似
# optuna>=3.0            # 贝叶斯超参数优化 BO（缺失则退化为随机搜索）
# statsmodels>=0.14      # ARIMA 经典统计时序模型
