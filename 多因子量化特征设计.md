# 多因子量化输入特征设计规则

统一原则：
1. **所有特征最终归一化到可比的数值区间**（推荐 z-score 或 [-1, 1] / [0, 1]），避免某一类特征因为量纲大（比如成交额是"亿"级别）而在模型里"喧宾夺主"。
2. **每个特征必须能标注生成时间戳**，且该时间戳必须早于或等于预测发生的时刻——这是防止"未来函数"最关键的一步。
3. 极端值要做 **截断（winsorize）或对数压缩**，比如新闻阅读量、成交量这种右偏分布，直接用原始数值会让极端事件权重过大。

---

## 一、价格因子（日/周/月K线）

| 特征名 | 计算方式 | 说明 |
|---|---|---|
| 日收益率 | `(close_t - close_t-1) / close_t-1` | 直接可用，无需额外量化 |
| N日动量 | `(close_t - close_t-N) / close_t-N`，N=5/10/20/60 | 分别对应周/双周/月/季度动量 |
| 波动率 | `std(过去N日收益率)` | 衡量风险，N建议20 |
| RSI | 标准14日RSI公式 | 已天然在0-100，可直接除以100归一化 |
| 量比 | `今日成交量 / 过去5日均量` | 用log(量比+1)压缩极端放量 |
| 周/月K线特征 | 同上公式，换算成对应周期 | 建议只取"周期收益率"和"周期波动率"两个，避免维度爆炸 |

---

## 二、资金流向因子

⚠️ 前面说过，这类数据本身是估算值（按大单/中单/小单拆分成交额去猜测"主力"行为），精度有限，建议**权重不要设得比价格因子高**。

| 特征名 | 计算方式 | 说明 |
|---|---|---|
| 净流入率 | `(大单买入额 - 大单卖出额) / 当日总成交额` | 天然在[-1,1]附近，不需要额外归一化 |
| 连续流入天数 | 过去N日中净流入为正的天数占比 | 0-1之间 |
| 北向资金变化率 | `(今日北向持仓 - 昨日北向持仓) / 昨日北向持仓` | 仅适用于有北向数据的标的 |

---

## 三、基本面/财报因子

原则：**用同比/环比变化率，而不是绝对值**（绝对值在不同市值公司间不可比）。

| 特征名 | 计算方式 | 说明 |
|---|---|---|
| 营收同比增速 | `(本期营收 - 去年同期营收) / abs(去年同期营收)` | 亏损公司用abs防止符号错乱 |
| 净利润同比增速 | 同上，替换为净利润 | 如果去年同期为负（亏损转盈利/加剧亏损），建议单独打标签而不是算比率，避免除以小负数导致数值爆炸 |
| ROE | 净利润 / 净资产 | 天然是比率，直接可用 |
| 资产负债率 | 负债总额 / 资产总额 | 0-1之间 |
| PE/PB相对行业分位数 | `该股PE在同行业过去1年PE的分位数(0-1)` | 比直接用PE数值更有可比性，因为不同行业估值中枢差异巨大 |
| **公司亏损量化** | 你提到的"亏损了多少就按实际写"：建议用 `本期净利润 / 上年营收` 而不是直接用绝对亏损金额，这样不同规模公司才可比 |

⚠️ **时间对齐关键点**：财报公布日通常滞后于报告期结束（比如Q3报告10月才发布），特征表里必须用"财报实际公布日期"而不是"报告期截止日期"来对齐，否则就是未来函数。

---

## 四、政策因子

政策类信息很难连续量化，建议用**事件打标 + 影响范围打分**的组合方式，而不是单一连续数值：

| 特征名 | 计算方式 | 说明 |
|---|---|---|
| 政策事件类型 | one-hot编码：`货币政策/产业政策/监管政策/无` | 分类变量 |
| 政策方向 | `+1（利好）/ -1（利空）/ 0（中性）` | 需要人工标注或规则库匹配关键词（比如"降准""减税""限产""处罚"） |
| 政策影响范围 | `0-1`，是否覆盖该股所在行业 | 1=直接点名行业，0.5=间接相关，0=无关 |
| 政策衰减因子 | `影响力 × exp(-t/τ)`，t=距发布天数，τ建议5-10天 | 政策效应会随时间衰减，不是永久生效 |

---

## 五、新闻/舆情因子

你提到的思路（阅读量 × 情感强度）方向是对的，但**阅读量不能直接线性使用**，因为新闻传播天然是幂律分布（少数新闻阅读量极高，大部分很低），建议：

```
新闻情感分 = sentiment_score × log(1 + 阅读量) × 时间衰减因子
```

具体拆解：

| 组件 | 计算方式 | 说明 |
|---|---|---|
| `sentiment_score` | 情感分析模型输出，范围建议 [-1, 1] | -1极度负面，+1极度正面。你举的例子"恐惧新闻"打-10，建议改成先在[-1,1]内打分，再乘权重，不要直接用-10这种没有边界的数值，否则一条极端新闻会主导整个特征 |
| `log(1+阅读量)` | 对阅读量取对数再+1防止log(0) | 10万阅读量和100万阅读量的"热度差"不该是10倍关系，log压缩后更合理 |
| 时间衰减因子 | `exp(-t/τ)`，τ建议1-3天（新闻比政策衰减快得多） | 3天前的新闻对今天的影响应该已经很小 |
| 多条新闻聚合 | 当日该股相关新闻取**加权平均**，而不是简单求和 | 求和会导致新闻数量多的股票天然分数虚高，跟真实情感无关 |

⚠️ **最容易踩的坑**：市场对新闻的反应经常是反直觉的（"利空出尽是利好"、"预期已经price in"）。单纯的情感极性分类容易把方向判反。建议这个特征先单独做一轮"加入后是否提升方向准确率"的对比实验（就是你已经在做的朴素基准对比方法），如果没有统计显著提升，说明目前的情感量化方式还不够好，需要迭代，而不是直接信任它。

---

## 六、汇总表结构建议

最终喂给模型的特征表，建议长这样（每行=一只股票在某一天的样本）：

| 日期 | 代码 | 价格因子×N | 资金流因子×N | 基本面因子×N | 政策因子×N | 新闻因子×N | **标签：次日涨跌方向** | **标签：次日涨跌幅%** |
|---|---|---|---|---|---|---|---|---|

分类标签建议同时保留**方向（涨/跌，二分类）**和**幅度（连续值，回归）**两套，因为：
- 方向预测更稳健，适合先验证模型有没有信号
- 幅度预测更有实用价值，但噪音更大，建议先把方向模型做稳了再挑战幅度

---

## 七、上线前必须做的三个校验

1. **特征重要性拆解**：训练完成后用SHAP或特征重要性排序，看看新闻/政策这类"难量化"的因子到底贡献了多少，如果贡献接近0，说明现在的量化方式还不成熟，别急着上线
2. **分批消融实验**：按你之前的思路，先用纯价格因子跑基线，再逐类加入资金流/基本面/政策/新闻，每加一类都对比方向准确率是否有统计显著提升（不是账面上涨了1%就算数，要看置信区间）
3. **时间外样本测试**：一定要用训练数据之后的时间段做测试（比如用2023年之前数据训练，2024年之后数据测试），而不是随机切分，否则很容易因为"未来信息泄露"制造虚假的高准确率

---

*本文档为特征工程方法论参考，具体阈值（如衰减系数τ、分位数窗口N）需要结合实际回测效果调整，不是固定公式。*
